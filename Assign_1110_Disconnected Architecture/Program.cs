using Assign_1110_Disconnected_Architecture;
using System.Security.AccessControl;

DataAccess access = new DataAccess();

//access.AddEntry();

//access.Search(50);

//access.Delete(60);

//access.Update(60);

access.DataRelationObj();